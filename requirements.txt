# 有声书合成工作台 — Python 依赖
#
# 运行依赖（由 audiobook-studio 的 Python 3.10+ venv 提供）：
gradio>=5.50,<6

# 音频后处理 / 数值计算
numpy
scipy
pyloudnorm        # D1 响度 LUFS-16 归一（纯 CPU/numpy）
mutagen            # D2 ID3 / M4B 章节标签（纯 Python）

# 说明：
#   - torch / indextts / IndexTTS2 由 index-tts 的 venv 提供，不在此列出。
#   - ffmpeg 是系统二进制，需自行下载并加入 PATH（不能通过 pip 安装）。
#     下载：https://ffmpeg.org/download.html
#     缺失时导出会在运行时给出明确诊断。

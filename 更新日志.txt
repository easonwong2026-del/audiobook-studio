# 有声书合成工作台 — 更新日志

## [Storage Layout v3] — 2026-08（开发中，未发布）
- 项目根目录收敛为 4 个一级目录：`01_原始资料/ 02_生成音频/ 03_导出成品/ 99_系统数据/`。
- 系统 JSON 统一进 `99_系统数据/配置/`；章节数据、质检、任务库、缓存、日志、临时各归其位。
- 新项目创建即纯 v3 布局；v1/v2 旧项目保持兼容读取，可经书架「整理存储布局」显式迁移（plan → token → execute，迁移前自动备份）。
- 正式导出发布到 `03_导出成品/正式导出/`，失败不留半成品；补录 WAV 进 `02_生成音频/补录音频/`，补录导出进 `03_导出成品/补录/`。
- 新增「打开生成音频 / 打开导出成品」目录入口；Storage summary 显示 Storage Layout 版本。

## [Wave 3 / 下拉式试听 / 重合成 = 功能等价] — 2026-06
- 原 Tab2 日志行内嵌试听 / 重合成按钮的承诺，已收敛为 Tab3 段落下拉式试听 / 重合成（功能等价），消除文档漂移。日志区域保持只读 Textbox。

## [数据目录外置] — 2026-06
- 所有项目、合成产物、音色库、导出文件默认外置到用户数据目录（可配置）。
- config.json 已从版本控制中移除（.gitignore）。

## [V3.1 稳定化] — 2026-07
- UI 模块拆分、Repository 持久化层、ProjectSnapshot、统一错误处理、日志轮转。
- 补录合成、暂停/恢复、断点续跑、OOM 降级、LUFS 归一。
- 399 测试、CI 流水线（无 GPU/模型）。
